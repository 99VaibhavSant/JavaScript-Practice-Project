/*
implement basic pass/fail evaluation based on marks
Added a conditional block that checks whether the student's marks are
greater than or equal to 35. If true, it prints "Pass"; otherwise, it
prints "Fail". This helps future developers understand the grading logic.
*/

// Runs one block if true, otherwise runs the else part

let marks = 40;

if (marks >= 35) {
  console.log("Pass");
} else {
  console.log("Fail");
}
